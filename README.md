# npman
NicePakageManager is nice pakage manager

# Installation
```
git clone https://github.com/Shower5853/npman.git
cd npman
chmod +x npman
sudo mv npman /usr/bin
```

# Usage
Type
```
npman install <package_name>
```
to install package, other functions will be added soon!
